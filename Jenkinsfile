pipeline {
    agent {
        docker { image 'node:7-alpine' }
    }
    stages {
        stage('Build') { 
            steps {
                echo 'docker info'
            }
        }
        stage('Test') { 
            steps {
                echo 'docker info'
            }
        }
        stage('Deploy') { 
            steps {
               echo 'docker info'
            }
        }
    }
}
