Under `Dashboard → Manage Jenkins → System`

under the `Git` , add `Git Server`

name: as customize
URL : --default--
Credentials : select the `github-token` as credentials

once clicked on `Test Validation` will show status.

Go Ahead 
